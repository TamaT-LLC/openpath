/// アプリの識別情報。
/// `Resources/Info.plist` と同じ値を保つ必要があり、不一致はテストで検出する。
public enum AppInfo {
    public static let name = "openpath"
    public static let bundleIdentifier = "jp.tamat.openpath"
    public static let version = "0.1.0"
}
